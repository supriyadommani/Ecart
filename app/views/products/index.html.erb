<h1>Products</h1>
  <ul>
    <% @products.each do |product| %>
      <li> 
        <% link_to product.name, product %>
        <span><%= number_to_cuurency(product.price) %></span>
      </li>
    <%end %>
  </ul