<h1>Shopping cart</h1>
<% @cart.line_items.each do |item| %>
  <p><%= item.product.name %> - <%= number_to_currency(items.product.price) %> x <%= item..quantity %></p>

<% end %>
<%= link_to 'Checkout', new_order_path, class: "button" %>
